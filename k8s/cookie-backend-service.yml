apiVersion: v1
kind: Service
metadata:
  labels:
    app: cookie-backend
  name: cookie-backend
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: cookie-backend
  type: ClusterIP
status:
  loadBalancer: {}
