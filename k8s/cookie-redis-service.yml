apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: cookie-redis
  name: cookie-redis
spec:
  ports:
    - port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    app: cookie-redis
  type: ClusterIP
